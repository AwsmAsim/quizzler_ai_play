import 'dart:ui';

class ColorsConstants {
  // static Color primaryColor = Color(0xFF2A4B7C);
  // static Color thirdColor = Color(0xFFC5A46D);
  // static Color correctColor = Color(0xFF80C783);
  // static Color wrongColor = Color(0xFFFF8080);
  // static Color ctaColor = Color(0xFFC5A46D);

  static Color primaryColor =
      Color(0xFF6E41E2); // Bold purple (energetic yet professional)
  static Color secondaryColor = Color(0xFF00C1B6); // Teal (fresh contrast)
  static Color thirdColor =
      Color(0xFFFFA726); // Warm orange-gold (modern accent)
  static Color correctColor = Color(0xFF9CFF8E); // Vibrant lime green
  static Color wrongColor =
      Color(0xFFFF6F6F); // Coral red (softer but attention-grabbing)
  static Color ctaColor = Color(0xFFFFA726);
}

enum SettingsMenu { logout, editDetails, deleteAccount }

class SizeConstants {
  static double defaultPadding = 20.0;
}
